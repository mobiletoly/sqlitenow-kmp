This directory may contain a local `sqlitenow-compiler.jar` for focused
SwiftPM plugin validation.

Release SwiftPM distribution tags do not include that jar. They use the
`SQLiteNowCompiler` executable artifact bundle declared by the root package
manifest instead.
