-- @@{ enableSync=true }
CREATE TABLE file_reviews (
    id TEXT PRIMARY KEY NOT NULL,
    review TEXT NOT NULL,
    file_id TEXT NOT NULL REFERENCES files(id) ON DELETE CASCADE DEFERRABLE INITIALLY DEFERRED
);
