// Generated by SQLiteNow. Do not modify this file manually.
import 'dart:typed_data';

import 'package:sqlitenow_runtime/sqlitenow_runtime.dart';
import 'package:sqlitenow_oversqlite/sqlitenow_oversqlite.dart';

final class NowSampleSyncDatabaseAdapters {
  const NowSampleSyncDatabaseAdapters({
    required this.commentCreatedAtToSql,
    required this.sqlValueToCommentCreatedAt,
    required this.personBirthDateToSql,
    required this.sqlValueToPersonBirthDate,
    required this.personCreatedAtToSql,
    required this.sqlValueToPersonCreatedAt,
    required this.personUpdatedAtToSql,
    required this.sqlValueToPersonUpdatedAt,
    required this.personIsActiveToSql,
    required this.sqlValueToPersonIsActive,
    required this.personAddressAddressTypeToSql,
    required this.sqlValueToPersonAddressAddressType,
    required this.personAddressIsPrimaryToSql,
    required this.sqlValueToPersonAddressIsPrimary,
    required this.personAddressCreatedAtToSql,
    required this.sqlValueToPersonAddressCreatedAt,
  });

  final Object? Function(Object? value) commentCreatedAtToSql;
  final Object? Function(Object? value) sqlValueToCommentCreatedAt;

  final Object? Function(Object? value) personBirthDateToSql;
  final Object? Function(Object? value) sqlValueToPersonBirthDate;

  final Object? Function(Object? value) personCreatedAtToSql;
  final Object? Function(Object? value) sqlValueToPersonCreatedAt;

  final Object? Function(Object? value) personUpdatedAtToSql;
  final Object? Function(Object? value) sqlValueToPersonUpdatedAt;

  final Object? Function(Object? value) personIsActiveToSql;
  final Object? Function(Object? value) sqlValueToPersonIsActive;

  final Object? Function(Object? value) personAddressAddressTypeToSql;
  final Object? Function(Object? value) sqlValueToPersonAddressAddressType;

  final Object? Function(Object? value) personAddressIsPrimaryToSql;
  final Object? Function(Object? value) sqlValueToPersonAddressIsPrimary;

  final Object? Function(Object? value) personAddressCreatedAtToSql;
  final Object? Function(Object? value) sqlValueToPersonAddressCreatedAt;
}

final class CommentRow {
  const CommentRow({
    required this.id,
    required this.personId,
    required this.comment,
    required this.createdAt,
    required this.tags,
  });

  final String id;
  final Uint8List personId;
  final String comment;
  final DateTime createdAt;
  final String? tags;
}

final class PersonRow {
  const PersonRow({
    required this.id,
    required this.myFirstName,
    required this.myLastName,
    required this.email,
    required this.phone,
    required this.birthDate,
    required this.createdAt,
    required this.updatedAt,
    required this.ssn,
    required this.score,
    required this.isActive,
    required this.notes,
  });

  final Uint8List id;
  final String myFirstName;
  final String myLastName;
  final String email;
  final String? phone;
  final DateTime? birthDate;
  final DateTime createdAt;
  final DateTime updatedAt;
  final int? ssn;
  final double? score;
  final bool isActive;
  final String? notes;
}

final class PersonAddressRow {
  const PersonAddressRow({
    required this.id,
    required this.personId,
    required this.addressType,
    required this.street,
    required this.city,
    required this.state,
    required this.postalCode,
    required this.country,
    required this.isPrimary,
    required this.createdAt,
  });

  final Uint8List id;
  final Uint8List personId;
  final String addressType;
  final String street;
  final String city;
  final String? state;
  final String? postalCode;
  final String country;
  final bool isPrimary;
  final DateTime createdAt;
}

final class CommentAddParams {
  const CommentAddParams({
    required this.id,
    required this.personId,
    required this.comment,
    required this.createdAt,
    required this.tags,
  });

  final String id;
  final Uint8List personId;
  final String comment;
  final DateTime createdAt;
  final String? tags;
}

final class CommentSelectAllParams {
  const CommentSelectAllParams({required this.personId});

  final Uint8List personId;
}

final class PersonAddParams {
  const PersonAddParams({
    required this.email,
    required this.firstName,
    required this.lastName,
    required this.phone,
    required this.birthDate,
    required this.ssn,
    required this.score,
    required this.isActive,
    required this.notes,
  });

  final String email;
  final String firstName;
  final String lastName;
  final String? phone;
  final DateTime? birthDate;
  final int? ssn;
  final double? score;
  final bool isActive;
  final String? notes;
}

final class PersonDeleteByIdParams {
  const PersonDeleteByIdParams({required this.id});

  final Uint8List id;
}

final class PersonSelectAllParams {
  const PersonSelectAllParams({required this.limit, required this.offset});

  final int limit;
  final int offset;
}

final class PersonUpdateByIdParams {
  const PersonUpdateByIdParams({
    required this.firstName,
    required this.lastName,
    required this.email,
    required this.phone,
    required this.birthDate,
    required this.ssn,
    required this.score,
    required this.isActive,
    required this.notes,
    required this.id,
  });

  final String firstName;
  final String lastName;
  final String email;
  final String? phone;
  final DateTime? birthDate;
  final int? ssn;
  final double? score;
  final bool isActive;
  final String? notes;
  final Uint8List id;
}

final class PersonAddressAddParams {
  const PersonAddressAddParams({
    required this.personId,
    required this.addressType,
    required this.street,
    required this.city,
    required this.state,
    required this.postalCode,
    required this.country,
    required this.isPrimary,
  });

  final Uint8List personId;
  final String addressType;
  final String street;
  final String city;
  final String? state;
  final String? postalCode;
  final String country;
  final bool isPrimary;
}

final class PersonAddressSelectAllParams {
  const PersonAddressSelectAllParams({required this.personId});

  final Uint8List personId;
}

final class NowSampleSyncDatabase {
  NowSampleSyncDatabase.inMemory({
    required NowSampleSyncDatabaseAdapters adapters,
  }) : this._(SqliteNowDatabase.inMemory(migrations: _migrations), adapters);

  NowSampleSyncDatabase({
    required String path,
    required NowSampleSyncDatabaseAdapters adapters,
  }) : this._(SqliteNowDatabase(path: path, migrations: _migrations), adapters);

  NowSampleSyncDatabase._(this._database, this.adapters) {
    comment = CommentQueries(this);
    person = PersonQueries(this);
    personAddress = PersonAddressQueries(this);
  }

  final SqliteNowDatabase _database;
  final NowSampleSyncDatabaseAdapters adapters;
  late final CommentQueries comment;
  late final PersonQueries person;
  late final PersonAddressQueries personAddress;

  bool get isOpen => _database.isOpen;
  SqliteNowConnection get connection => _database.connection;
  Future<void> open() => _database.open();
  Future<void> close() => _database.close();
  Future<T> transaction<T>(
    Future<T> Function() block, {
    TransactionMode mode = TransactionMode.deferred,
  }) {
    return _database.transaction(block, mode: mode);
  }

  void reportExternalTableChanges(Set<String> affectedTables) {
    _database.reportExternalTableChanges(affectedTables);
  }

  SqliteNowDatabase get runtimeDatabase => _database;

  static const List<SyncTable> syncTables = <SyncTable>[
    SyncTable(tableName: 'comment', syncKeyColumnName: 'id'),
    SyncTable(tableName: 'person', syncKeyColumnName: 'id'),
    SyncTable(tableName: 'person_address', syncKeyColumnName: 'id'),
  ];

  OversqliteConfig buildOversqliteConfig({
    required String schema,
    List<SyncTable> syncTables = NowSampleSyncDatabase.syncTables,
    int uploadLimit = 200,
    int downloadLimit = 1000,
    bool verboseLogs = false,
    Duration automaticDownloadInterval = const Duration(seconds: 60),
    BundleChangeWatchMode bundleChangeWatchMode = BundleChangeWatchMode.off,
    Duration bundleChangeWatchReconnectMin = const Duration(seconds: 1),
    Duration bundleChangeWatchReconnectMax = const Duration(seconds: 60),
  }) {
    return OversqliteConfig(
      schema: schema,
      syncTables: syncTables,
      uploadLimit: uploadLimit,
      downloadLimit: downloadLimit,
      verboseLogs: verboseLogs,
      automaticDownloadInterval: automaticDownloadInterval,
      bundleChangeWatchMode: bundleChangeWatchMode,
      bundleChangeWatchReconnectMin: bundleChangeWatchReconnectMin,
      bundleChangeWatchReconnectMax: bundleChangeWatchReconnectMax,
    );
  }

  DefaultOversqliteClient newOversqliteClient({
    required String schema,
    required OversqliteHttpClient httpClient,
    List<SyncTable> syncTables = NowSampleSyncDatabase.syncTables,
    int uploadLimit = 200,
    int downloadLimit = 1000,
    bool verboseLogs = false,
    Duration automaticDownloadInterval = const Duration(seconds: 60),
    BundleChangeWatchMode bundleChangeWatchMode = BundleChangeWatchMode.off,
    Duration bundleChangeWatchReconnectMin = const Duration(seconds: 1),
    Duration bundleChangeWatchReconnectMax = const Duration(seconds: 60),
  }) {
    final config = buildOversqliteConfig(
      schema: schema,
      syncTables: syncTables,
      uploadLimit: uploadLimit,
      downloadLimit: downloadLimit,
      verboseLogs: verboseLogs,
      automaticDownloadInterval: automaticDownloadInterval,
      bundleChangeWatchMode: bundleChangeWatchMode,
      bundleChangeWatchReconnectMin: bundleChangeWatchReconnectMin,
      bundleChangeWatchReconnectMax: bundleChangeWatchReconnectMax,
    );
    return DefaultOversqliteClient(
      database: _database,
      config: config,
      httpClient: httpClient,
    );
  }

  static final List<SqliteNowMigrationStep> _migrations = [
    SqliteNowMigrationStep(1, (connection) async {
      await connection.execute(r'''CREATE TABLE comment
(
    id         TEXT PRIMARY KEY NOT NULL,
    person_id  BLOB             NOT NULL,
    comment    TEXT             NOT NULL,

    -- @@{ field=created_at, adapter=custom, propertyType=DateTime }
    created_at TEXT             NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ', 'now')),
    tags       TEXT,

    FOREIGN KEY (person_id) REFERENCES person (id) ON DELETE CASCADE
)
WITHOUT ROWID;''');
      await connection.execute(r'''CREATE TABLE person
(
    id         BLOB PRIMARY KEY NOT NULL DEFAULT (randomblob(16)),
    -- @@{ field=first_name, propertyName=myFirstName }
    first_name TEXT                NOT NULL,
    -- @@{ field=last_name, propertyName=myLastName }
    last_name  TEXT                NOT NULL,

    email      TEXT                NOT NULL UNIQUE,
    phone      TEXT,

    -- @@{ field=birth_date, adapter=custom, propertyType=DateTime }
    birth_date TEXT,
    -- @@{ field=created_at, adapter=custom, propertyType=DateTime }
    created_at TEXT                NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ', 'now')),
    -- @@{ field=updated_at, adapter=custom, propertyType=DateTime }
    updated_at TEXT                NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ', 'now')),

    ssn        INTEGER,
    score      REAL,
    -- @@{ field=is_active, adapter=custom, propertyType=bool }
    is_active  INTEGER             NOT NULL DEFAULT 1,

    notes      TEXT
)
WITHOUT ROWID;''');
      await connection.execute(
        r'''CREATE INDEX idx_person_name ON person (last_name, first_name);''',
      );
      await connection.execute(
        r'''CREATE INDEX idx_person_email ON person (email);''',
      );
      await connection.execute(r'''CREATE TABLE person_address
(
    id           BLOB PRIMARY KEY NOT NULL DEFAULT (randomblob(16)),
    person_id    BLOB              NOT NULL,

    -- @@{ field=address_type, adapter=custom, propertyType=String }
    address_type TEXT              NOT NULL,
    street       TEXT              NOT NULL,
    city         TEXT              NOT NULL,
    state        TEXT,
    postal_code  TEXT,
    country      TEXT              NOT NULL,

    -- @@{ field=is_primary, adapter=custom, propertyType=bool }
    is_primary   INTEGER           NOT NULL DEFAULT 0,
    -- @@{ field=created_at, adapter=custom, propertyType=DateTime }
    created_at   TEXT              NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%SZ', 'now')),

    FOREIGN KEY (person_id) REFERENCES person (id) ON DELETE CASCADE
)
WITHOUT ROWID;''');
      await connection.execute(
        r'''CREATE INDEX idx_personAddress_personId ON person_address (person_id);''',
      );
      await connection.execute(
        r'''CREATE INDEX idx_personAddress_personId_isPrimary ON person_address (person_id, is_primary);''',
      );
    }),
  ];
}

final class CommentQueries {
  CommentQueries(this._db);

  final NowSampleSyncDatabase _db;

  Future<void> add(CommentAddParams params) async {
    await _db.connection.execute(
      r'''INSERT INTO comment (id, person_id, comment, created_at, tags) VALUES (?, ?, ?, ?, ?)''',
      parameters: [
        params.id,
        params.personId,
        params.comment,
        _db.adapters.commentCreatedAtToSql(params.createdAt),
        params.tags,
      ],
      affectedTables: const <String>{'comment'},
    );
  }

  SelectRunner<CommentRow> selectAll(CommentSelectAllParams params) {
    return SelectRunner<CommentRow>(
      database: _db.runtimeDatabase,
      affectedTables: const <String>{'comment'},
      query: () {
        return _db.connection.select(
          r'''SELECT id, person_id, comment, created_at, tags FROM comment WHERE person_id = ? ORDER BY created_at DESC''',
          (row) => CommentRow(
            id: row.readString(0),
            personId: row.readBlob(1),
            comment: row.readString(2),
            createdAt:
                _db.adapters.sqlValueToCommentCreatedAt(row.readString(3))
                    as DateTime,
            tags: row.readNullableString(4),
          ),
          parameters: [params.personId],
        );
      },
    );
  }
}

final class PersonQueries {
  PersonQueries(this._db);

  final NowSampleSyncDatabase _db;

  Future<void> add(PersonAddParams params) async {
    await _db.connection.execute(
      r'''INSERT INTO person (email, first_name, last_name, phone, birth_date, ssn, score, is_active, notes) VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?) ON CONFLICT (  email )  DO UPDATE SET first_name = ?, last_name = ?, phone = ?, birth_date = ?, ssn = ?, score = ?, is_active = ?, notes = ?, updated_at = (strftime('%Y-%m-%dT%H:%M:%SZ', 'now'))''',
      parameters: [
        params.email,
        params.firstName,
        params.lastName,
        params.phone,
        _db.adapters.personBirthDateToSql(params.birthDate),
        params.ssn,
        params.score,
        _db.adapters.personIsActiveToSql(params.isActive),
        params.notes,
        params.firstName,
        params.lastName,
        params.phone,
        _db.adapters.personBirthDateToSql(params.birthDate),
        params.ssn,
        params.score,
        _db.adapters.personIsActiveToSql(params.isActive),
        params.notes,
      ],
      affectedTables: const <String>{'person'},
    );
  }

  Future<void> deleteById(PersonDeleteByIdParams params) async {
    await _db.connection.execute(
      r'''DELETE FROM person WHERE id = ?''',
      parameters: [params.id],
      affectedTables: const <String>{'person'},
    );
  }

  SelectRunner<PersonRow> selectAll(PersonSelectAllParams params) {
    return SelectRunner<PersonRow>(
      database: _db.runtimeDatabase,
      affectedTables: const <String>{'person'},
      query: () {
        return _db.connection.select(
          r'''SELECT * FROM person ORDER BY id DESC LIMIT ? OFFSET ?''',
          (row) => PersonRow(
            id: row.readBlob(0),
            myFirstName: row.readString(1),
            myLastName: row.readString(2),
            email: row.readString(3),
            phone: row.readNullableString(4),
            birthDate:
                _db.adapters.sqlValueToPersonBirthDate(
                      row.readNullableString(5),
                    )
                    as DateTime?,
            createdAt:
                _db.adapters.sqlValueToPersonCreatedAt(row.readString(6))
                    as DateTime,
            updatedAt:
                _db.adapters.sqlValueToPersonUpdatedAt(row.readString(7))
                    as DateTime,
            ssn: row.readNullableInt(8),
            score: row.readNullableDouble(9),
            isActive:
                _db.adapters.sqlValueToPersonIsActive(row.readInt(10)) as bool,
            notes: row.readNullableString(11),
          ),
          parameters: [params.limit, params.offset],
        );
      },
    );
  }

  Future<void> updateById(PersonUpdateByIdParams params) async {
    await _db.connection.execute(
      r'''UPDATE person SET first_name = ?, last_name = ?, email = ?, phone = ?, birth_date = ?, ssn = ?, score = ?, is_active = ?, notes = ?, updated_at = (strftime('%Y-%m-%dT%H:%M:%SZ', 'now')) WHERE id = ?''',
      parameters: [
        params.firstName,
        params.lastName,
        params.email,
        params.phone,
        _db.adapters.personBirthDateToSql(params.birthDate),
        params.ssn,
        params.score,
        _db.adapters.personIsActiveToSql(params.isActive),
        params.notes,
        params.id,
      ],
      affectedTables: const <String>{'person'},
    );
  }
}

final class PersonAddressQueries {
  PersonAddressQueries(this._db);

  final NowSampleSyncDatabase _db;

  Future<void> add(PersonAddressAddParams params) async {
    await _db.connection.execute(
      r'''INSERT INTO person_address (person_id, address_type, street, city, state, postal_code, country, is_primary) VALUES (?, ?, ?, ?, ?, ?, ?, ?)''',
      parameters: [
        params.personId,
        _db.adapters.personAddressAddressTypeToSql(params.addressType),
        params.street,
        params.city,
        params.state,
        params.postalCode,
        params.country,
        _db.adapters.personAddressIsPrimaryToSql(params.isPrimary),
      ],
      affectedTables: const <String>{'person_address'},
    );
  }

  SelectRunner<PersonAddressRow> selectAll(
    PersonAddressSelectAllParams params,
  ) {
    return SelectRunner<PersonAddressRow>(
      database: _db.runtimeDatabase,
      affectedTables: const <String>{'person_address'},
      query: () {
        return _db.connection.select(
          r'''SELECT * FROM person_address WHERE person_id = ? ORDER BY created_at DESC''',
          (row) => PersonAddressRow(
            id: row.readBlob(0),
            personId: row.readBlob(1),
            addressType:
                _db.adapters.sqlValueToPersonAddressAddressType(
                      row.readString(2),
                    )
                    as String,
            street: row.readString(3),
            city: row.readString(4),
            state: row.readNullableString(5),
            postalCode: row.readNullableString(6),
            country: row.readString(7),
            isPrimary:
                _db.adapters.sqlValueToPersonAddressIsPrimary(row.readInt(8))
                    as bool,
            createdAt:
                _db.adapters.sqlValueToPersonAddressCreatedAt(row.readString(9))
                    as DateTime,
          ),
          parameters: [params.personId],
        );
      },
    );
  }
}
