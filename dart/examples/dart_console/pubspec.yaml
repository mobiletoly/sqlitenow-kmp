name: sqlitenow_console_example
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  sqlitenow_runtime: ^0.9.0

dev_dependencies:
  sqlitenow_cli: ^0.9.0
  test: ">=1.30.0 <1.31.0"
