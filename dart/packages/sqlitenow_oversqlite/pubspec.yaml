name: sqlitenow_oversqlite
description: Dart Oversqlite sync runtime for SQLiteNow generated sync metadata.
version: 0.9.0
homepage: https://github.com/mobiletoly/sqlitenow-kmp
repository: https://github.com/mobiletoly/sqlitenow-kmp
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  crypto: ^3.0.7
  sqlitenow_runtime: ^0.9.0

dev_dependencies:
  lints: ^6.1.0
  test: ">=1.30.0 <1.31.0"
